length = float(input("Specify room's length in meters: "))
width = float(input("Specify room's width in meters: "))
area = length * width
print(f"Room's area: {area:.2f} square meters and {area * 10.7693:.2f} square feet.")
